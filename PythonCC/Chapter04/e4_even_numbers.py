#  使用range()创建数字列表，使用list()函数将range()的结果直接转换为列表。
#  将range()函数作为list()的参数，输出一个数字列表
numbers = list(range(1, 6))
print(numbers)

#  指定步长，第三个参数
even_numbers = list(range(2, 11, 2))
print(even_numbers)

#  使用range()几乎能够创建任何需要的数字集
